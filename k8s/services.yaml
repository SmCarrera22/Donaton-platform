apiVersion: v1
kind: Service
metadata:
  name: ms-user
  namespace: donaton
spec:
  selector:
    app: ms-user
  ports:
    - port: 8081
      targetPort: 8081
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: ms-auth
  namespace: donaton
spec:
  selector:
    app: ms-auth
  ports:
    - port: 8082
      targetPort: 8082
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: ms-donation
  namespace: donaton
spec:
  selector:
    app: ms-donation
  ports:
    - port: 8083
      targetPort: 8083
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: ms-campaign
  namespace: donaton
spec:
  selector:
    app: ms-campaign
  ports:
    - port: 8084
      targetPort: 8084
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: bff
  namespace: donaton
spec:
  selector:
    app: bff
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: donaton
spec:
  selector:
    app: api-gateway
  ports:
    - port: 8090
      targetPort: 8090
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: donaton
spec:
  selector:
    app: frontend
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080
  type: NodePort